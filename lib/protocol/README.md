# protocol/

Binary wire protocol: magic, header fields, CRC16, packet types.

| Packet types | HELLO, PING, PONG, MESSAGE, ACK, NACK, DEVICE_INFO, KEEPALIVE, ERROR, CONFIG_SYNC |

See `docs/PHASE1_ARCHITECTURE.md` §4 for wire format.
